TODO
refactor class Preprocess to functions -> avoid shared state btwn process

explore threads instead of process

split work into crop() half of image each process instead of just 256*256 square each time 



===============================================================================================
TRIED

 adding spawn to map init ->    with get_context("spawn").Pool() as pool:
    - should of avoided unecessary forking overhead of unused parent resources/file descriptors
    

